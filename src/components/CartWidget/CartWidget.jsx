import cart from './cart.png';

const CartWidget = () => {
    return (
        <div>
            <img className="cartlogo" src= {cart} alt="cart" />
        </div>
        )
}

export default CartWidget;